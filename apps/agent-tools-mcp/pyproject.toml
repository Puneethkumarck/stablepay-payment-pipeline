[project]
name = "stablepay-agent-tools-mcp"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["stablepay-schemas-py"]

[tool.uv.sources]
stablepay-schemas-py = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
