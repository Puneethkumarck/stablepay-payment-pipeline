services:
  kafka:
    environment:
      KAFKA_LOG_DIRS: /tmp/kraft-ci-logs
    tmpfs:
      - /tmp/kraft-ci-logs
  opensearch:
    environment:
      OPENSEARCH_JAVA_OPTS: "-Xms256m -Xmx256m"
      DISABLE_SECURITY_PLUGIN: "true"
      discovery.type: single-node
