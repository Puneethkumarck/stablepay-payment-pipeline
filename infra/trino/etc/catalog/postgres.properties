# Admin / ops audience only. This catalog connects with the application's
# stablepay role (full read/write on auth, idempotency, application tables) and
# performs no per-customer scoping. Do not expose the postgres catalog to
# customer-facing dashboards — server-side customer scope (CLAUDE.md hard
# constraint #7) is bypassed here.
connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/stablepay
connection-user=stablepay
connection-password=stablepay_dev
